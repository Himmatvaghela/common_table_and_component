import { ReadObjectPipe } from './read-object.pipe';

describe('ReadObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
